@* <div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <nav id="sidebar" class="col-md-3 col-lg-2 d-md-block bg-light sidebar">
            <div class="position-sticky">
                <h4 class="sidebar-heading">Categories</h4>
                <ul class="nav flex-column">
                    @foreach (var category in await CategoryService.GetAllCategoriesAsync())
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Book" asp-action="CategoryBooks" asp-route-categoryId="@category.Id">
                                @category.Name
                            </a>
                        </li>
                    }
                </ul>
            </div>
        </nav>

        <!-- Main content -->
        <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
            @RenderBody()
        </main>
    </div>
</div>
 *@